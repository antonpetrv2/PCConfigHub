[build]
  command = "npm run build --workspace pc-config-hub-web"
  publish = "pc-config-hub-web/.next"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--workspaces=false --include=optional"

[[plugins]]
  package = "@netlify/plugin-nextjs"
